{
  "name": "@ed/standards-data",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Versioned standards and manufacturer datasets with page-level provenance.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@ed/core": "workspace:*",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
